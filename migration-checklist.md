# Чек-лист миграции на унифицированные инструменты

## 📋 Общий чек-лист проекта

### Подготовительный этап
- [ ] **Аудит кода** - проведён анализ всех используемых инструментов
- [ ] **Резервное копирование** - созданы резервные копии всего кода
- [ ] **Изучение документации** - изучены migration-guide.md и tool-mapping.md
- [ ] **Настройка среды** - настроен слой обратной совместимости
- [ ] **План миграции** - составлен поэтапный план миграции
- [ ] **Команда готова** - все разработчики ознакомлены с новой архитектурой

### Этап 1: Миграция управления задачами (task_manage)
- [ ] **addTask → task_manage**
  - [ ] Заменены все вызовы `addTask({ ... })` на `task_manage({ action: "add", ... })`
  - [ ] Проверены параметры: title, description, priority, dependsOn, relatedFiles, tests
  - [ ] Протестирована функциональность создания задач
  
- [ ] **addSubtask → task_manage**
  - [ ] Заменены все вызовы `addSubtask({ ... })` на `task_manage({ action: "add_subtask", ... })`
  - [ ] Проверены параметры: parentId, title, relatedFiles, tests
  - [ ] Протестирована функциональность создания подзадач

- [ ] **listTasks → task_manage**
  - [ ] Заменены все вызовы `listTasks({ ... })` на `task_manage({ action: "list", ... })`
  - [ ] Проверены параметры: status, format
  - [ ] Протестированы различные форматы вывода (json, table, human)

- [ ] **updateTask → task_manage**
  - [ ] Заменены все вызовы `updateTask({ ... })` на `task_manage({ action: "update", ... })`
  - [ ] Проверены параметры: id, title, description, priority, dependsOn, relatedFiles, tests
  - [ ] Протестирована функциональность обновления задач

- [ ] **updateStatus → task_manage**
  - [ ] Заменены все вызовы `updateStatus({ ... })` на `task_manage({ action: "update_status", ... })`
  - [ ] Проверены параметры: id, newStatus, message
  - [ ] Протестированы все статусы: todo, inprogress, testing, done, blocked, error

- [ ] **removeTask → task_manage**
  - [ ] Заменены все вызовы `removeTask({ ... })` на `task_manage({ action: "remove", ... })`
  - [ ] Проверены параметры: id
  - [ ] Протестирована функциональность удаления задач и подзадач

- [ ] **getContext → task_manage**
  - [ ] Заменены все вызовы `getContext({ ... })` на `task_manage({ action: "context", ... })`
  - [ ] Проверены параметры: id
  - [ ] Протестирована функциональность получения контекста

- [ ] **getNextTask → task_manage**
  - [ ] Заменены все вызовы `getNextTask()` на `task_manage({ action: "next" })`
  - [ ] Протестирована логика выбора следующей задачи

### Этап 2: Миграция файловых операций (file_system)
- [ ] **readFile → file_system**
  - [ ] Заменены все вызовы `readFile({ ... })` на `file_system({ action: "read", ... })`
  - [ ] Проверены параметры: path
  - [ ] Протестировано чтение различных типов файлов

- [ ] **readMultipleFiles → file_system**
  - [ ] Заменены все вызовы `readMultipleFiles({ ... })` на `file_system({ action: "read", paths: [...] })`
  - [ ] Проверены параметры: paths (массив)
  - [ ] Протестировано чтение нескольких файлов одновременно

- [ ] **writeFile → file_system**
  - [ ] Заменены все вызовы `writeFile({ ... })` на `file_system({ action: "write", ... })`
  - [ ] Проверены параметры: path, content
  - [ ] Протестирована запись файлов с различным содержимым

- [ ] **copyFile → file_system**
  - [ ] Заменены все вызовы `copyFile({ ... })` на `file_system({ action: "copy", ... })`
  - [ ] Проверены параметры: source, destination
  - [ ] Протестировано копирование файлов и директорий

- [ ] **moveFile → file_system**
  - [ ] Заменены все вызовы `moveFile({ ... })` на `file_system({ action: "move", ... })`
  - [ ] Проверены параметры: source, destination
  - [ ] Протестировано перемещение и переименование

- [ ] **deleteFile → file_system**
  - [ ] Заменены все вызовы `deleteFile({ ... })` на `file_system({ action: "delete", ... })`
  - [ ] Проверены параметры: path, recursive
  - [ ] Протестировано удаление файлов и директорий

- [ ] **listDirectory → file_system**
  - [ ] Заменены все вызовы `listDirectory({ ... })` на `file_system({ action: "list", ... })`
  - [ ] Проверены параметры: path
  - [ ] Протестировано получение списка файлов

- [ ] **createDirectory → file_system**
  - [ ] Заменены все вызовы `createDirectory({ ... })` на `file_system({ action: "create", ... })`
  - [ ] Проверены параметры: path
  - [ ] Протестировано создание директорий

- [ ] **tree → file_system**
  - [ ] Заменены все вызовы `tree({ ... })` на `file_system({ action: "tree", ... })`
  - [ ] Проверены параметры: path, depth, follow_symlinks
  - [ ] Протестировано построение дерева директорий

- [ ] **searchFiles → file_system**
  - [ ] Заменены все вызовы `searchFiles({ ... })` на `file_system({ action: "search", ... })`
  - [ ] Проверены параметры: path, pattern
  - [ ] Протестирован поиск файлов по паттернам

- [ ] **getFileInfo → file_system**
  - [ ] Заменены все вызовы `getFileInfo({ ... })` на `file_system({ action: "info", ... })`
  - [ ] Проверены параметры: path
  - [ ] Протестировано получение информации о файлах

- [ ] **getFilesystemStatus → file_system**
  - [ ] Заменены все вызовы `getFilesystemStatus()` на `file_system({ action: "status" })`
  - [ ] Протестировано получение статуса файловой системы

### Этап 3: Миграция браузерной автоматизации (browser_control)

#### Навигация
- [ ] **browserNavigate → browser_control**
  - [ ] Заменены все вызовы `browserNavigate({ ... })` на `browser_control({ action: "navigate", operation: "navigate", ... })`
  - [ ] Проверены параметры: url
  - [ ] Протестирована навигация по URL

- [ ] **browserNavigateBack → browser_control**
  - [ ] Заменены все вызовы `browserNavigateBack()` на `browser_control({ action: "navigate", operation: "back" })`
  - [ ] Протестирована навигация назад

- [ ] **browserNavigateForward → browser_control**
  - [ ] Заменены все вызовы `browserNavigateForward()` на `browser_control({ action: "navigate", operation: "forward" })`
  - [ ] Протестирована навигация вперёд

#### Взаимодействие
- [ ] **browserClick → browser_control**
  - [ ] Заменены все вызовы `browserClick({ ... })` на `browser_control({ action: "interact", operation: "click", ... })`
  - [ ] Проверены параметры: element, ref
  - [ ] Протестированы клики по различным элементам

- [ ] **browserType → browser_control**
  - [ ] Заменены все вызовы `browserType({ ... })` на `browser_control({ action: "interact", operation: "type", ... })`
  - [ ] Проверены параметры: element, ref, text, slowly, submit
  - [ ] Протестирован ввод текста в различные поля

- [ ] **browserHover → browser_control**
  - [ ] Заменены все вызовы `browserHover({ ... })` на `browser_control({ action: "interact", operation: "hover", ... })`
  - [ ] Проверены параметры: element, ref
  - [ ] Протестировано наведение на элементы

- [ ] **browserDrag → browser_control**
  - [ ] Заменены все вызовы `browserDrag({ ... })` на `browser_control({ action: "interact", operation: "drag", ... })`
  - [ ] Проверены параметры: startElement, startRef, endElement, endRef
  - [ ] Протестировано перетаскивание элементов

- [ ] **browserSelectOption → browser_control**
  - [ ] Заменены все вызовы `browserSelectOption({ ... })` на `browser_control({ action: "interact", operation: "select", ... })`
  - [ ] Проверены параметры: element, ref, values
  - [ ] Протестирован выбор опций в выпадающих списках

- [ ] **browserPressKey → browser_control**
  - [ ] Заменены все вызовы `browserPressKey({ ... })` на `browser_control({ action: "interact", operation: "key", ... })`
  - [ ] Проверены параметры: key
  - [ ] Протестировано нажатие клавиш

#### Захват контента
- [ ] **browserTakeScreenshot → browser_control**
  - [ ] Заменены все вызовы `browserTakeScreenshot({ ... })` на `browser_control({ action: "capture", operation: "screenshot", ... })`
  - [ ] Проверены параметры: filename, element, ref, raw
  - [ ] Протестировано создание скриншотов

- [ ] **browserSnapshot → browser_control**
  - [ ] Заменены все вызовы `browserSnapshot()` на `browser_control({ action: "capture", operation: "snapshot" })`
  - [ ] Протестировано создание accessibility снимков

- [ ] **browserPdfSave → browser_control**
  - [ ] Заменены все вызовы `browserPdfSave({ ... })` на `browser_control({ action: "capture", operation: "pdf", ... })`
  - [ ] Проверены параметры: filename
  - [ ] Протестировано сохранение в PDF

- [ ] **browserConsoleMessages → browser_control**
  - [ ] Заменены все вызовы `browserConsoleMessages()` на `browser_control({ action: "capture", operation: "console" })`
  - [ ] Протестировано получение сообщений консоли

- [ ] **browserNetworkRequests → browser_control**
  - [ ] Заменены все вызовы `browserNetworkRequests()` на `browser_control({ action: "capture", operation: "network" })`
  - [ ] Протестировано получение сетевых запросов

#### Загрузка файлов
- [ ] **browserFileUpload → browser_control**
  - [ ] Заменены все вызовы `browserFileUpload({ ... })` на `browser_control({ action: "upload", ... })`
  - [ ] Проверены параметры: paths
  - [ ] Протестирована загрузка файлов

#### Ожидание
- [ ] **browserWait → browser_control**
  - [ ] Заменены все вызовы `browserWait({ ... })` на `browser_control({ action: "wait", operation: "time", ... })`
  - [ ] Проверены параметры: time
  - [ ] Протестировано ожидание по времени

- [ ] **browserWaitFor → browser_control**
  - [ ] Заменены все вызовы `browserWaitFor({ ... })` на `browser_control({ action: "wait", operation: "text", ... })`
  - [ ] Проверены параметры: text, textGone, time
  - [ ] Протестировано ожидание текста

#### Управление
- [ ] **browserResize → browser_control**
  - [ ] Заменены все вызовы `browserResize({ ... })` на `browser_control({ action: "manage", operation: "resize", ... })`
  - [ ] Проверены параметры: width, height
  - [ ] Протестировано изменение размера окна

- [ ] **browserHandleDialog → browser_control**
  - [ ] Заменены все вызовы `browserHandleDialog({ ... })` на `browser_control({ action: "manage", operation: "dialog", ... })`
  - [ ] Проверены параметры: accept, promptText
  - [ ] Протестирована обработка диалогов

- [ ] **browserClose → browser_control**
  - [ ] Заменены все вызовы `browserClose()` на `browser_control({ action: "manage", operation: "close" })`
  - [ ] Протестировано закрытие браузера

- [ ] **browserInstall → browser_control**
  - [ ] Заменены все вызовы `browserInstall()` на `browser_control({ action: "manage", operation: "install" })`
  - [ ] Протестирована установка браузера

#### Управление вкладками
- [ ] **browserTabList → browser_control**
  - [ ] Заменены все вызовы `browserTabList()` на `browser_control({ action: "manage", operation: "tabs", subOperation: "list" })`
  - [ ] Протестировано получение списка вкладок

- [ ] **browserTabNew → browser_control**
  - [ ] Заменены все вызовы `browserTabNew({ ... })` на `browser_control({ action: "manage", operation: "tabs", subOperation: "new", ... })`
  - [ ] Проверены параметры: url
  - [ ] Протестировано создание новых вкладок

- [ ] **browserTabSelect → browser_control**
  - [ ] Заменены все вызовы `browserTabSelect({ ... })` на `browser_control({ action: "manage", operation: "tabs", subOperation: "select", ... })`
  - [ ] Проверены параметры: index
  - [ ] Протестировано переключение между вкладками

- [ ] **browserTabClose → browser_control**
  - [ ] Заменены все вызовы `browserTabClose({ ... })` на `browser_control({ action: "manage", operation: "tabs", subOperation: "close", ... })`
  - [ ] Проверены параметры: index
  - [ ] Протестировано закрытие вкладок

### Этап 4: Тестирование и валидация
- [ ] **Модульные тесты**
  - [ ] Все модульные тесты обновлены для новых инструментов
  - [ ] Добавлены тесты для новой функциональности
  - [ ] Все тесты проходят успешно

- [ ] **Интеграционные тесты**
  - [ ] Протестированы сценарии взаимодействия между инструментами
  - [ ] Проверена работа слоя совместимости
  - [ ] Протестированы граничные случаи

- [ ] **Производительность**
  - [ ] Измерена производительность до и после миграции
  - [ ] Подтверждено улучшение производительности на 50-70%
  - [ ] Подтверждено сокращение использования памяти на 60-75%

- [ ] **Функциональность**
  - [ ] Все существующие функции работают корректно
  - [ ] Форматы ответов соответствуют ожиданиям
  - [ ] Обработка ошибок работает правильно

### Этап 5: Завершение миграции
- [ ] **Отключение слоя совместимости**
  - [ ] Все старые инструменты заменены на новые
  - [ ] Слой совместимости отключён
  - [ ] Проведено финальное тестирование без слоя совместимости

- [ ] **Очистка кода**
  - [ ] Удалены неиспользуемые импорты старых инструментов
  - [ ] Обновлены комментарии и документация в коде
  - [ ] Проведён code review

- [ ] **Документация**
  - [ ] Обновлена техническая документация проекта
  - [ ] Созданы примеры использования новых инструментов
  - [ ] Обновлены README файлы

- [ ] **Мониторинг**
  - [ ] Настроен мониторинг производительности
  - [ ] Настроены алерты для критических ошибок
  - [ ] Проведён анализ логов

## 🔍 Чек-лист для code review

### Общие проверки
- [ ] Нет вызовов старых инструментов (addTask, readFile, browserClick и т.д.)
- [ ] Все новые инструменты используют action-based подход
- [ ] Параметры соответствуют новой схеме
- [ ] Обработка ошибок адаптирована под новые форматы ответов

### Специфичные проверки
- [ ] **task_manage**: все действия указывают корректный action
- [ ] **file_system**: пути к файлам корректно обрабатываются
- [ ] **browser_control**: action и operation указаны правильно
- [ ] Асинхронные вызовы правильно обрабатываются
- [ ] Типизация (если используется TypeScript) обновлена

### Производительность
- [ ] Нет избыточных вызовов инструментов
- [ ] Используется кэширование где это уместно
- [ ] Батчинг операций где это возможно

## 📊 Метрики успешности миграции

### Количественные метрики
- [ ] **Сокращение инструментов**: достигнуто 76% сокращение (78 → 19)
- [ ] **Производительность**: улучшение на 50-70%
- [ ] **Память**: сокращение использования на 60-75%
- [ ] **Покрытие тестами**: не менее 85%

### Качественные метрики
- [ ] **Стабильность**: отсутствие критических ошибок
- [ ] **Поддерживаемость**: упрощение архитектуры
- [ ] **Документированность**: полная документация новых инструментов
- [ ] **Готовность команды**: все разработчики обучены новой архитектуре

## 🚨 Критические проверки перед продакшеном

- [ ] **Резервное копирование**: созданы полные резервные копии
- [ ] **План отката**: подготовлен план возврата к старой версии
- [ ] **Мониторинг**: настроен расширенный мониторинг
- [ ] **Команда готова**: дежурная команда знает новую архитектуру
- [ ] **Документация**: вся документация обновлена
- [ ] **Тестирование**: проведено полное тестирование в staging среде

## ✅ Подтверждение завершения

**Дата начала миграции**: ___________  
**Дата завершения миграции**: ___________  
**Ответственный за миграцию**: ___________  
**Подпись**: ___________  

**Результаты:**
- Количество мигрированных файлов: ___________
- Количество замененных инструментов: ___________
- Улучшение производительности: ___________%
- Сокращение использования памяти: ___________%

**Комментарии и замечания:**
_________________________________________________
_________________________________________________
_________________________________________________

---

*Этот чек-лист должен быть полностью выполнен перед переходом на новую архитектуру в продакшене.*