# 🔄 GopiAI Model Switching System

Система переключения провайдеров и моделей для GopiAI с поддержкой Gemini и OpenRouter.

## 📋 Основные возможности

- **Переключение провайдеров**: Легкое переключение между Google Gemini и OpenRouter
- **Синхронизация состояния**: Сохранение текущего провайдера и модели в `~/.gopiai_state.json`
- **REST API**: Эндпоинты для управления состоянием и получения моделей
- **UI интеграция**: Виджет выбора модели с автоматическим обновлением
- **Rate limiting**: Система контроля лимитов и мягкого черного списка
- **API ключи**: Управление ключами через `.env` файл

## 🚀 Быстрый старт

### 1. Запуск системы

```bash
# Через Python
cd GopiAI-CrewAI
python start_model_switching_system.py

# Или через bat файл (Windows)
start_model_switching_system.bat
```

### 2. Использование UI

1. Откройте виджет выбора модели в интерфейсе GopiAI
2. Выберите провайдера (Gemini или OpenRouter)
3. Выберите модель из списка доступных
4. Сохраните API ключ при необходимости

## 🛠️ REST API

### Получение текущего состояния
```http
GET http://localhost:5051/internal/state
```

### Обновление состояния
```http
POST http://localhost:5051/internal/state
Content-Type: application/json

{
  "provider": "gemini",
  "model_id": "gemini/gemini-1.5-flash"
}
```

### Получение моделей по провайдеру
```http
GET http://localhost:5051/internal/models?provider=gemini
```

## 📁 Структура файлов

```
GopiAI-CrewAI/
├── llm_rotation_config.py     # Основная конфигурация моделей
├── state_manager.py           # Управление состоянием
├── crewai_api_server.py       # API сервер
├── start_model_switching_system.py  # Скрипт запуска
├── test_model_switching.py    # Тесты
└── ~/.gopiai_state.json       # Файл состояния (автосоздание)
```

## ⚙️ Конфигурация

### API ключи (.env файл)
```env
GEMINI_API_KEY=your_gemini_api_key
OPENROUTER_API_KEY=your_openrouter_api_key
```

### Поддерживаемые модели

#### Google Gemini
- `gemini/gemini-1.5-flash` - Быстрая модель для общих задач
- `gemini/gemini-2.0-flash-lite` - Улучшенная версия Flash
- `gemini/gemini-3` - Специализированная модель для простых задач

#### OpenRouter
- `openrouter/google-gemma-2b-it` - Легкая модель Google Gemma
- `openrouter/mistralai-mistral-7b-instruct` - Популярная модель Mistral

## 🧪 Тестирование

```bash
python test_model_switching.py
```

## 🐛 Решение проблем

### Проблемы с переключением провайдеров
1. Проверьте файл `~/.gopiai_state.json`
2. Убедитесь, что API ключи указаны в `.env`
3. Перезапустите сервер после изменений

### Проблемы с моделями
1. Проверьте доступность API ключей
2. Убедитесь, что модель поддерживает ваш тип задач
3. Проверьте лимиты использования модели

## 📊 Мониторинг

Система автоматически отслеживает:
- Использование RPM/TPM/RPD
- Превышение лимитов (мягкий черный список)
- Доступность моделей
- Статус провайдеров

## 🔄 Как это работает

1. **UI взаимодействие**: Виджет отправляет POST запросы на `/internal/state`
2. **Сервер обновляет состояние**: Сохраняет выбор в `~/.gopiai_state.json`
3. **Backend использует состояние**: `llm_rotation_config.py` читает текущий выбор
4. **Синхронизация**: Все компоненты используют одно и то же состояние

## 📈 Rate Limiting

Система автоматически:
- Отслеживает использование моделей (RPM, TPM, RPD)
- Блокирует модели при превышении лимитов
- Автоматически разблокирует модели через заданное время
- Предоставляет информацию о статусе блокировки

## 🔧 Разработка

### Добавление новых моделей
1. Добавьте конфигурацию в `MODELS` в `llm_rotation_config.py`
2. Укажите параметры: `rpm`, `tpm`, `rpd`, `priority`
3. Перезапустите систему

### Добавление новых провайдеров
1. Добавьте маппинг в `PROVIDER_KEY_ENV`
2. Добавьте модели в `MODELS`
3. Обновите UI виджет при необходимости

## 📄 Лицензия

MIT License - см. LICENSE файл для подробностей.
