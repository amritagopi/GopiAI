# 🔥 КРИТИЧЕСКИЕ ЗАДАЧИ ДЛЯ СПАСЕНИЯ GopiAI

## 🚨 ПРИОРИТЕТ 1: ИСПРАВЛЕНИЕ СИСТЕМЫ ИНСТРУМЕНТОВ

### Task 1.1: Удаление сломанного парсера команд
**Статус:** 🔴 Критично  
**Время:** 2-3 часа  
**Файлы:** `GopiAI-CrewAI/tools/gopiai_integration/smart_delegator.py`, `command_executor.py`

**Действия:**
1. Найти и удалить регулярку `lss*([^n]*)` и весь код парсинга команд из текста
2. Удалить функции типа `parse_command_from_text()` 
3. Найти логи `[PARSER] Найдена текстовая команда:` и удалить эту логику
4. Очистить `CommandExecutor` от текстового парсинга

**Критерий готовности:** Нет упоминаний регулярок для парсинга команд в коде

### Task 1.2: Создание JSON-схемы инструментов
**Статус:** 🔴 Критично  
**Время:** 1-2 часа  
**Файлы:** `GopiAI-CrewAI/tools/tool_definitions.py` (новый)

**Действия:**
1. Создать файл `tool_definitions.py` с функцией `get_tool_schema()`
2. Описать все инструменты в стандартном формате OpenAI:
   - `execute_terminal_command`
   - `browse_website` 
   - `web_search`
   - `filesystem_tools`
3. Каждый инструмент должен иметь name, description, parameters

**Пример кода:**
```python
def get_tool_schema():
    return [
        {
            "type": "function",
            "function": {
                "name": "execute_terminal_command",
                "description": "Выполняет команду в терминале",
                "parameters": {
                    "type": "object",
                    "properties": {
                        "command": {
                            "type": "string",
                            "description": "Команда для выполнения"
                        }
                    },
                    "required": ["command"]
                }
            }
        }
    ]
```

### Task 1.3: Переписать вызов LLM с нативным Tool Calling
**Статус:** 🔴 Критично  
**Время:** 3-4 часа  
**Файлы:** `smart_delegator.py`, `_call_llm` функция

**Действия:**
1. Импортировать `get_tool_schema()` 
2. Модифицировать `litellm.completion()` для передачи `tools` и `tool_choice="auto"`
3. Обработать `response.choices[0].message.tool_calls`
4. Реализовать двухэтапный процесс: вызов инструмента → финальный ответ
5. Добавить результаты инструментов в историю сообщений

**Критерий готовности:** LLM использует нативный Tool Calling вместо парсинга текста

### Task 1.4: Переписать CommandExecutor
**Статус:** 🔴 Критично  
**Время:** 2-3 часа  
**Файлы:** `command_executor.py`

**Действия:**
1. Изменить интерфейс: `execute(function_name, **function_args)` вместо парсинга текста
2. Создать маппинг имен функций на реальные методы
3. Убрать все регулярки и текстовый парсинг
4. Добавить валидацию аргументов

**Пример:**
```python
def execute(self, function_name: str, **kwargs):
    if function_name == "execute_terminal_command":
        return self.run_terminal_command(kwargs["command"])
    elif function_name == "browse_website":
        return self.browse_url(kwargs["url"])
```

## 🛡️ ПРИОРИТЕТ 2: СТАБИЛИЗАЦИЯ И ОБРАБОТКА ОШИБОК

### Task 2.1: Добавить обработку ошибок LLM
**Статус:** 🟡 Важно  
**Время:** 1-2 часа  
**Файлы:** `smart_delegator.py`

**Действия:**
1. Обернуть `litellm.completion()` в `try...except`
2. Ловить `RateLimitError`, `AuthenticationError`, `Exception`
3. Возвращать осмысленные ошибки вместо пустых ответов
4. Добавить логирование всех ошибок

**Пример:**
```python
try:
    response = litellm.completion(...)
except litellm.RateLimitError:
    return {"error": "Достигнут лимит запросов. Попробуйте позже."}
except Exception as e:
    logger.error(f"LLM error: {e}")
    return {"error": "Внутренняя ошибка LLM"}
```

### Task 2.2: Исправить краш при создании вкладок
**Статус:** 🟡 Важно  
**Время:** 1 час  
**Файлы:** `rich_text_notebook_widget.py`, `tab_widget.py`

**Действия:**
1. В методах создания вкладок сохранять ссылки на виджеты в `self.tabs = []`
2. Убедиться что виджеты не удаляются сборщиком мусора
3. Добавить `try...except` в методы создания вкладок
4. Проверить передачу `parent` параметра

### Task 2.3: Исправить плавающий терминал
**Статус:** 🟡 Важно  
**Время:** 30 минут  
**Файлы:** `terminal_widget.py`, код создания терминала

**Действия:**
1. Найти место создания `TerminalWidget`
2. Передать `parent=self.main_tab_widget` при создании
3. Убрать флаги типа `Qt.Window`
4. Добавить терминал во вкладку: `addTab(terminal, "Терминал")`

### Task 2.4: Настроить whitelist команд
**Статус:** 🟡 Важно  
**Время:** 15 минут  
**Файлы:** `command_executor.py`

**Действия:**
1. Найти список `ALLOWED_COMMANDS` или подобный
2. Добавить базовые команды: `['ls', 'dir', 'cat', 'type', 'echo', 'pwd', 'cd']`
3. Убрать опасные команды: `rm`, `del`, `mv` (если есть)

## 🧹 ПРИОРИТЕТ 3: ОЧИСТКА И ОПТИМИЗАЦИЯ

### Task 3.1: Удалить мусорные файлы
**Статус:** 🟢 Низкий  
**Время:** 15 минут  

**Действия:**
1. Найти и удалить `example_dynamic_instructions.py`
2. Найти и удалить `example_usage.py`
3. Проверить отсутствие импортов этих файлов
4. Очистить неиспользуемые импорты

### Task 3.2: Установить недостающие зависимости
**Статус:** 🟢 Низкий  
**Время:** 10 минут  

**Действия:**
```bash
pip install txtai[faiss]
pip install --upgrade openai
pip install --upgrade litellm
```

### Task 3.3: Улучшить логирование
**Статус:** 🟢 Низкий  
**Время:** 30 минут  

**Действия:**
1. Добавить подробное логирование в критические места
2. Логировать все вызовы инструментов
3. Логировать ошибки с полным traceback
4. Настроить уровни логирования

## 📋 ПЛАН ВЫПОЛНЕНИЯ

### День 1 (4-6 часов): Экстренные исправления
- ✅ Task 1.1: Удаление сломанного парсера
- ✅ Task 1.2: Создание JSON-схемы инструментов  
- ✅ Task 1.3: Переписать вызов LLM
- ✅ Task 2.4: Настроить whitelist команд

### День 2 (3-4 часа): Стабилизация
- ✅ Task 1.4: Переписать CommandExecutor
- ✅ Task 2.1: Обработка ошибок LLM
- ✅ Task 2.2: Исправить краш вкладок
- ✅ Task 2.3: Исправить терминал

### День 3 (1 час): Финальная очистка
- ✅ Task 3.1: Удалить мусор
- ✅ Task 3.2: Установить зависимости
- ✅ Task 3.3: Улучшить логирование

## 🎯 КРИТЕРИИ УСПЕХА

После выполнения всех задач:
1. ✅ Инструменты работают без ошибок парсинга
2. ✅ Нет крашей при создании вкладок
3. ✅ LLM возвращает осмысленные ответы или ошибки
4. ✅ Терминал открывается во вкладке, а не в отдельном окне
5. ✅ Базовые команды (`ls`, `dir`) выполняются успешно

## 🚀 НАЧИНАЕМ С TASK 1.1!

Первым делом находим и уничтожаем сломанный парсер команд. Это решит 80% всех проблем!