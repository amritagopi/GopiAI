# 🔧 TASK 1.2: СОЗДАНИЕ JSON-СХЕМЫ ИНСТРУМЕНТОВ

## 🎯 ЦЕЛЬ
Создать стандартную JSON-схему инструментов в формате OpenAI для замены сломанного парсера текста на нативный Tool Calling.

## 📋 АНАЛИЗ СУЩЕСТВУЮЩИХ ИНСТРУМЕНТОВ

Из логов видно, что в системе есть следующие инструменты:
1. **Terminal/Command Execution** - выполнение команд в терминале
2. **Browser/Web Navigation** - открытие веб-страниц
3. **Web Search** - поиск в интернете
4. **File System Operations** - работа с файлами

## 📁 СТРУКТУРА ФАЙЛОВ

### Создать новый файл:
`GopiAI-CrewAI/tools/gopiai_integration/tool_definitions.py`

### Структура схемы:
```python
def get_tool_schema():
    """Возвращает схему всех доступных инструментов в формате OpenAI"""
    return [
        # Инструмент 1: Терминал
        # Инструмент 2: Браузер  
        # Инструмент 3: Поиск
        # Инструмент 4: Файловая система
    ]
```

## 🛠️ ДЕТАЛЬНЫЕ СХЕМЫ ИНСТРУМЕНТОВ

### 1. Terminal Command Executor
```python
{
    "type": "function",
    "function": {
        "name": "execute_terminal_command",
        "description": "Выполняет команду в терминале операционной системы. Поддерживает команды для навигации по файловой системе, установки пакетов, запуска скриптов и других системных операций.",
        "parameters": {
            "type": "object",
            "properties": {
                "command": {
                    "type": "string",
                    "description": "Команда для выполнения в терминале. Примеры: 'ls -la', 'pip install package', 'python script.py', 'cd /path/to/directory'"
                },
                "working_directory": {
                    "type": "string",
                    "description": "Рабочая директория для выполнения команды (опционально)",
                    "default": "."
                }
            },
            "required": ["command"]
        }
    }
}
```

### 2. Web Browser Navigation
```python
{
    "type": "function", 
    "function": {
        "name": "browse_website",
        "description": "Открывает веб-страницу по указанному URL и извлекает её содержимое. Может использоваться для чтения документации, статей, получения информации с веб-сайтов.",
        "parameters": {
            "type": "object",
            "properties": {
                "url": {
                    "type": "string",
                    "description": "URL веб-страницы для открытия. Должен начинаться с http:// или https://"
                },
                "extract_text": {
                    "type": "boolean", 
                    "description": "Извлекать только текстовое содержимое без HTML тегов",
                    "default": true
                }
            },
            "required": ["url"]
        }
    }
}
```

### 3. Web Search
```python
{
    "type": "function",
    "function": {
        "name": "web_search",
        "description": "Выполняет поиск в интернете по заданному запросу и возвращает релевантные результаты с заголовками, описаниями и ссылками.",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "Поисковый запрос на русском или английском языке"
                },
                "num_results": {
                    "type": "integer",
                    "description": "Количество результатов для возврата",
                    "default": 5,
                    "minimum": 1,
                    "maximum": 10
                }
            },
            "required": ["query"]
        }
    }
}
```

### 4. File System Operations
```python
{
    "type": "function",
    "function": {
        "name": "file_operations",
        "description": "Выполняет операции с файловой системой: чтение, запись, создание, удаление файлов и директорий.",
        "parameters": {
            "type": "object", 
            "properties": {
                "operation": {
                    "type": "string",
                    "enum": ["read", "write", "create_dir", "list_dir", "delete", "exists"],
                    "description": "Тип операции для выполнения"
                },
                "path": {
                    "type": "string",
                    "description": "Путь к файлу или директории"
                },
                "content": {
                    "type": "string", 
                    "description": "Содержимое для записи в файл (только для операции write)"
                },
                "encoding": {
                    "type": "string",
                    "description": "Кодировка файла",
                    "default": "utf-8"
                }
            },
            "required": ["operation", "path"]
        }
    }
}
```

## 📝 ПОЛНЫЙ КОД ФАЙЛА

### `tool_definitions.py`:
```python
"""
Определения инструментов для GopiAI в формате OpenAI Tool Calling
================================================================

Этот модуль содержит схемы всех доступных инструментов системы.
Используется для нативного Tool Calling вместо парсинга текста.
"""

def get_tool_schema():
    """
    Возвращает полную схему всех доступных инструментов
    
    Returns:
        list: Список инструментов в формате OpenAI Function Calling
    """
    return [
        # 1. Терминальные команды
        {
            "type": "function",
            "function": {
                "name": "execute_terminal_command",
                "description": "Выполняет команду в терминале операционной системы",
                "parameters": {
                    "type": "object",
                    "properties": {
                        "command": {
                            "type": "string",
                            "description": "Команда для выполнения"
                        }
                    },
                    "required": ["command"]
                }
            }
        },
        
        # 2. Веб-браузер
        {
            "type": "function",
            "function": {
                "name": "browse_website", 
                "description": "Открывает веб-страницу и извлекает содержимое",
                "parameters": {
                    "type": "object",
                    "properties": {
                        "url": {
                            "type": "string",
                            "description": "URL для открытия"
                        }
                    },
                    "required": ["url"]
                }
            }
        },
        
        # 3. Веб-поиск
        {
            "type": "function",
            "function": {
                "name": "web_search",
                "description": "Выполняет поиск в интернете",
                "parameters": {
                    "type": "object",
                    "properties": {
                        "query": {
                            "type": "string", 
                            "description": "Поисковый запрос"
                        },
                        "num_results": {
                            "type": "integer",
                            "default": 5
                        }
                    },
                    "required": ["query"]
                }
            }
        },
        
        # 4. Файловые операции
        {
            "type": "function",
            "function": {
                "name": "file_operations",
                "description": "Операции с файловой системой",
                "parameters": {
                    "type": "object",
                    "properties": {
                        "operation": {
                            "type": "string",
                            "enum": ["read", "write", "list_dir", "exists"]
                        },
                        "path": {
                            "type": "string"
                        },
                        "content": {
                            "type": "string"
                        }
                    },
                    "required": ["operation", "path"]
                }
            }
        }
    ]

def get_tool_by_name(name: str):
    """Получить схему конкретного инструмента по имени"""
    tools = get_tool_schema()
    for tool in tools:
        if tool["function"]["name"] == name:
            return tool
    return None

def get_available_tools():
    """Получить список имен всех доступных инструментов"""
    return [tool["function"]["name"] for tool in get_tool_schema()]
```

## 🧪 ТЕСТИРОВАНИЕ

### 1. Тест импорта
```python
# Создать test_tool_definitions.py
from tool_definitions import get_tool_schema, get_available_tools

def test_schema_structure():
    schema = get_tool_schema()
    assert isinstance(schema, list)
    assert len(schema) > 0
    
    for tool in schema:
        assert "type" in tool
        assert "function" in tool
        assert "name" in tool["function"]
        assert "description" in tool["function"]
        assert "parameters" in tool["function"]

def test_available_tools():
    tools = get_available_tools()
    expected = [
        "execute_terminal_command",
        "browse_website", 
        "web_search",
        "file_operations"
    ]
    assert all(tool in tools for tool in expected)
```

### 2. Тест валидности JSON
```python
import json

def test_json_serializable():
    schema = get_tool_schema()
    # Должно сериализоваться без ошибок
    json_str = json.dumps(schema, ensure_ascii=False, indent=2)
    # И десериализоваться обратно
    parsed = json.loads(json_str)
    assert parsed == schema
```

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

- [ ] Создан файл `tool_definitions.py`
- [ ] Реализована функция `get_tool_schema()`
- [ ] Добавлены все 4 основных инструмента
- [ ] Каждый инструмент имеет name, description, parameters
- [ ] Параметры имеют типы и описания
- [ ] Указаны required поля
- [ ] Добавлены вспомогательные функции
- [ ] Написаны тесты
- [ ] Схема валидна как JSON
- [ ] Нет синтаксических ошибок Python

## 🎯 РЕЗУЛЬТАТ

После выполнения этой задачи:
1. ✅ Есть стандартная схема инструментов в формате OpenAI
2. ✅ Подготовлена база для нативного Tool Calling
3. ✅ Четко определены интерфейсы всех инструментов
4. ✅ Можно легко добавлять новые инструменты

**Время выполнения:** 1-2 часа  
**Сложность:** Низкая  
**Критичность:** 🔴 Высокая

---

## 🚀 СЛЕДУЮЩИЙ ШАГ: TASK 1.3
После создания схемы переходим к переписыванию вызова LLM для использования нативного Tool Calling.