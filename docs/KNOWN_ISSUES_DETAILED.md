# Детальный отчёт о текущих проблемах

## 1. Не запуск сервера CrewAI
- При запуске GopiAI сервер CrewAI не стартует за отведенные 30 секунд.
- В логе `crewai_server.log` видно сообщение `[CREWAI] ❌ Сервер CrewAI не запустился за 30 секунд`.
- Возможные причины: отсутствие необходимых зависимостей, конфликт портов, или ошибка инициализации.

## 2. Недостающие модули
- [WARNING] ansi2html module not found, using fallback implementation — модуль `ansi2html` отсутствует, используется fallback, может влиять на визуализацию логов.
- [WARNING] Using fallback ModelProvider as gopiai_integration was removed — удалённый интеграционный модуль, используется fallback, может приводить к некорректному выбору модели.

## 3. Ошибки путей
- [WARNING] Путь не существует: /home/amritagopi/GopiAI/test_infrastructure/tests и подпапки — ожидаемые тесты не найдены, может влиять на тестовый раннер.

## 4. Проблемы UI
- QTextCursor::setPosition: Position '1' out of range несколько раз — ошибки в UI, вероятно из‑за некорректных индексов в текстовых полях.

## 5. Недоступность CrewAI API
- В асинхронных логах [DEBUG-ASYNC-BG-ERROR] CrewAI API недоступен! — клиент не может связаться с сервером, вероятно из-за того же сбоя запуска.

### Рекомендации
- Проверить, что порт 8000 (или указанный) свободен и не используется другими процессами.
- Установить недостающие зависимости: `pip install ansi2html` и убедиться, что `gopiai_integration` доступен или обновить конфигурацию.
- Исправить пути в конфигурации `test_infrastructure` или создать необходимые директории.
- Просмотреть стек трейс в `crewai_server.log` для более детальной диагностики.
- Обновить UI‑компоненты, проверив индексы в `QTextCursor` вызовах.