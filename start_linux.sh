#!/bin/bash

# Простой скрипт запуска GopiAI с автоматическим запуском сервера CrewAI
# Теперь UI сам запускает сервер CrewAI, поэтому этот скрипт стал намного проще

echo "🚀 Запуск GopiAI (с автоматическим запуском сервера CrewAI)..."

# Активируем виртуальное окружение если оно есть
if [ -d ".venv" ]; then
    echo "📦 Активация виртуального окружения..."
    source .venv/bin/activate
fi

# Добавляем пути в PYTHONPATH
export PYTHONPATH="${PYTHONPATH}:$(pwd)/GopiAI-CrewAI"

# Функция для корректного завершения
cleanup() {
    echo -e "\n🛑 Завершение работы..."
    # UI сам остановит сервер CrewAI через atexit
    exit 0
}

# Устанавливаем обработчик сигналов
trap cleanup SIGINT SIGTERM

# Запускаем UI (который автоматически запустит сервер CrewAI)
echo "🎨 Запуск GopiAI UI (сервер CrewAI запустится автоматически)..."
python -m gopiai.ui.main

echo "✅ GopiAI завершил работу"