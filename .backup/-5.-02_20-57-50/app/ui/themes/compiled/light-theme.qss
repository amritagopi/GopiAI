:root {
    /* Base Colors */
    --primary: #2980B9;
    --primary-variant: #3498DB;
    --secondary: #16A085;
    --accent: #E74C3C;
    --background: #F5F5F5;
    --surface: #FFFFFF;
    --background-variant: #EEEEEE;
    --on-primary: #FFFFFF;
    --on-secondary: #FFFFFF;
    --on-background: #333333;
    --on-surface: #333333;
    --error: #E74C3C;
    --warning: #F39C12;
    --success: #2ECC71;
    --info: #3498DB;
    --border: #CCCCCC;
    --divider: #DDDDDD;
    --disabled: #AAAAAA;
    --hover: #E0E0E0;
    --selected: #C0E0FF;

    /* Component Colors */
    --button-background: #EEEEEE; /* from @background-variant */
    --button-text: #333333; /* from @on-background */
    --button-hover: #2980B9; /* from @primary */
    --button-hover-text: #FFFFFF; /* from @on-primary */
    --button-pressed: #3498DB; /* from @primary-variant */
    --button-disabled: #AAAAAA; /* from @disabled */
    --input-background: #FFFFFF; /* from @surface */
    --input-text: #333333; /* from @on-surface */
    --input-border: #CCCCCC; /* from @border */
    --input-focus-border: #2980B9; /* from @primary */
    --tab-background: #EEEEEE; /* from @background-variant */
    --tab-text: #333333; /* from @on-background */
    --tab-selected: #2980B9; /* from @primary */
    --tab-selected-text: #FFFFFF; /* from @on-primary */
    --dock-title: #EEEEEE; /* from @background-variant */
    --dock-border: #CCCCCC; /* from @border */
    --menu-background: #F5F5F5; /* from @background */
    --menu-text: #333333; /* from @on-background */
    --menu-hover: #2980B9; /* from @primary */
    --menu-hover-text: #FFFFFF; /* from @on-primary */
    --terminal-background: #F0F0F0;
    --terminal-text: #000000;
    --scrollbar-background: #F5F5F5; /* from @background */
    --scrollbar-handle: #EEEEEE; /* from @background-variant */
    --scrollbar-hover: #2980B9; /* from @primary */
}

:root {
    /* Base Colors */
    --primary: #2980B9;
    --primary-variant: #3498DB;
    --secondary: #16A085;
    --accent: #E74C3C;
    --background: #F5F5F5;
    --surface: #FFFFFF;
    --background-variant: #EEEEEE;
    --on-primary: #FFFFFF;
    --on-secondary: #FFFFFF;
    --on-background: #333333;
    --on-surface: #333333;
    --error: #E74C3C;
    --warning: #F39C12;
    --success: #2ECC71;
    --info: #3498DB;
    --border: #CCCCCC;
    --divider: #DDDDDD;
    --disabled: #AAAAAA;
    --hover: #E0E0E0;
    --selected: #C0E0FF;

    /* Component Colors */
    --button-background: #EEEEEE; /* from @background-variant */
    --button-text: #333333; /* from @on-background */
    --button-hover: #2980B9; /* from @primary */
    --button-hover-text: #FFFFFF; /* from @on-primary */
    --button-pressed: #3498DB; /* from @primary-variant */
    --button-disabled: #AAAAAA; /* from @disabled */
    --input-background: #FFFFFF; /* from @surface */
    --input-text: #333333; /* from @on-surface */
    --input-border: #CCCCCC; /* from @border */
    --input-focus-border: #2980B9; /* from @primary */
    --tab-background: #EEEEEE; /* from @background-variant */
    --tab-text: #333333; /* from @on-background */
    --tab-selected: #2980B9; /* from @primary */
    --tab-selected-text: #FFFFFF; /* from @on-primary */
    --dock-title: #EEEEEE; /* from @background-variant */
    --dock-border: #CCCCCC; /* from @border */
    --menu-background: #F5F5F5; /* from @background */
    --menu-text: #333333; /* from @on-background */
    --menu-hover: #2980B9; /* from @primary */
    --menu-hover-text: #FFFFFF; /* from @on-primary */
    --terminal-background: #F0F0F0;
    --terminal-text: #000000;
    --scrollbar-background: #F5F5F5; /* from @background */
    --scrollbar-handle: #EEEEEE; /* from @background-variant */
    --scrollbar-hover: #2980B9; /* from @primary */
}

/* Файл исправлен css_fixer.py - устранены дублирующиеся селекторы */

:root {
    --accent: #E74C3C;
    --background: #F5F5F5;
    --background-variant: #EEEEEE;
    --border: #CCCCCC;
    --disabled: #AAAAAA;
    --divider: #DDDDDD;
    --error: #E74C3C;
    --hover: #E0E0E0;
    --info: #3498DB;
    --on-background: #333333;
    --on-primary: #FFFFFF;
    --on-secondary: #FFFFFF;
    --on-surface: #333333;
    --primary-variant: #3498DB;
    --scrollbar-background: #F5F5F5;
    --secondary: #16A085;
    --selected: #C0E0FF;
    --success: #2ECC71;
    --surface: #FFFFFF;
    --terminal-text: #000000;
    --warning: #F39C12;
    /* Base Colors */
    --primary: #2980B9;
    /* Component Colors */
    --button-background: #EEEEEE;
    /* from @background */
    --menu-text: #333333;
    /* from @background */
    --scrollbar-handle: #EEEEEE;
    /* from @background-variant */
    --button-text: #333333;
    /* from @background-variant */
    --dock-border: #CCCCCC;
    /* from @background-variant */
    --scrollbar-hover: #2980B9;
    /* from @background-variant */
    --tab-text: #333333;
    /* from @border */
    --input-focus-border: #2980B9;
    /* from @border */
    --menu-background: #F5F5F5;
    /* from @disabled */
    --input-background: #FFFFFF;
    /* from @on-background */
    --button-hover: #2980B9;
    /* from @on-background */
    --menu-hover: #2980B9;
    /* from @on-background */
    --tab-selected: #2980B9;
    /* from @on-primary */
    --button-pressed: #3498DB;
    /* from @on-primary */
    --dock-title: #EEEEEE;
    /* from @on-primary */
    --terminal-background: #F0F0F0;
    /* from @on-surface */
    --input-border: #CCCCCC;
    /* from @primary */
    --button-hover-text: #FFFFFF;
    /* from @primary */
    --menu-hover-text: #FFFFFF;
    /* from @primary */
    --tab-background: #EEEEEE;
    /* from @primary */
    --tab-selected-text: #FFFFFF;
    /* from @primary-variant */
    --button-disabled: #AAAAAA;
    /* from @surface */
    --input-text: #333333;
}

/* Файл исправлен css_fixer.py - устранены дублирующиеся селекторы */

:root {
    --accent: #E74C3C;
    --background: #F5F5F5;
    --background-variant: #EEEEEE;
    --border: #CCCCCC;
    --disabled: #AAAAAA;
    --divider: #DDDDDD;
    --error: #E74C3C;
    --hover: #E0E0E0;
    --info: #3498DB;
    --on-background: #333333;
    --on-primary: #FFFFFF;
    --on-secondary: #FFFFFF;
    --on-surface: #333333;
    --primary-variant: #3498DB;
    --scrollbar-background: #F5F5F5;
    --secondary: #16A085;
    --selected: #C0E0FF;
    --success: #2ECC71;
    --surface: #FFFFFF;
    --terminal-text: #000000;
    --warning: #F39C12;
    /* Base Colors */
    --primary: #2980B9;
    /* Component Colors */
    --button-background: #EEEEEE;
    /* from @background */
    --menu-text: #333333;
    /* from @background */
    --scrollbar-handle: #EEEEEE;
    /* from @background-variant */
    --button-text: #333333;
    /* from @background-variant */
    --dock-border: #CCCCCC;
    /* from @background-variant */
    --scrollbar-hover: #2980B9;
    /* from @background-variant */
    --tab-text: #333333;
    /* from @border */
    --input-focus-border: #2980B9;
    /* from @border */
    --menu-background: #F5F5F5;
    /* from @disabled */
    --input-background: #FFFFFF;
    /* from @on-background */
    --button-hover: #2980B9;
    /* from @on-background */
    --menu-hover: #2980B9;
    /* from @on-background */
    --tab-selected: #2980B9;
    /* from @on-primary */
    --button-pressed: #3498DB;
    /* from @on-primary */
    --dock-title: #EEEEEE;
    /* from @on-primary */
    --terminal-background: #F0F0F0;
    /* from @on-surface */
    --input-border: #CCCCCC;
    /* from @primary */
    --button-hover-text: #FFFFFF;
    /* from @primary */
    --menu-hover-text: #FFFFFF;
    /* from @primary */
    --tab-background: #EEEEEE;
    /* from @primary */
    --tab-selected-text: #FFFFFF;
    /* from @primary-variant */
    --button-disabled: #AAAAAA;
    /* from @surface */
    --input-text: #333333;
}

/* Светлая тема для GopiAI */

/* CSS-переменные для светлой темы */
* {
    --accent-color: #2980B9;
    --accent-color-pressed: #3498DB;
    --bg-primary: #F5F5F5;
    --bg-secondary: #FFFFFF;
    --bg-tertiary: #DDDDDD;
    --border-color: #CCCCCC;
    --fg-primary: #333333;
    --fg-secondary: #000000;
    --selection-bg: #ADD6FF;
    --terminal-bg: #F0F0F0;
}

/* Основные стили */
QWidget {
    background-color: var(--bg-primary);
    color: var(--fg-primary);
}

/* Кнопки */
QPushButton {
    background-color: var(--bg-tertiary);
    border: 1px solid var(--border-color);
    border-radius: 4px;
    color: var(--fg-primary);
    padding: 6px 12px;
}

QPushButton:hover {
    background-color: var(--accent-color);
    color: var(--bg-secondary);
}

QPushButton:pressed {
    background-color: var(--accent-color-pressed);
    color: var(--bg-secondary);
}

/* Поля ввода */
QLineEdit, QTextEdit, QPlainTextEdit {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 4px;
    color: var(--fg-primary);
    padding: 4px;
}

/* Вкладки */
QTabWidget::pane {
    border: 1px solid var(--border-color);
}

QTabBar::tab {
    background-color: var(--bg-tertiary);
    border-top-left-radius: 4px;
    border-top-right-radius: 4px;
    color: var(--fg-primary);
    margin-right: 2px;
    padding: 6px 12px;
}

QTabBar::tab:selected {
    background-color: var(--accent-color);
    color: var(--bg-secondary);
}

/* Полосы прокрутки */
QScrollBar:vertical {
    background-color: var(--bg-primary);
    margin: 0px;
    width: 12px;
}

QScrollBar::handle:vertical {
    background-color: var(--bg-tertiary);
    border-radius: 6px;
    min-height: 20px;
}

QScrollBar::handle:vertical:hover {
    background-color: var(--accent-color);
}

QScrollBar:horizontal {
    background-color: var(--bg-primary);
    height: 12px;
    margin: 0px;
}

QScrollBar::handle:horizontal {
    background-color: var(--bg-tertiary);
    border-radius: 6px;
    min-width: 20px;
}

QScrollBar::handle:horizontal:hover {
    background-color: var(--accent-color);
}

/* Списки и таблицы */
QListView, QTreeView, QTableView {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border-color);
    color: var(--fg-primary);
    selection-background-color: var(--accent-color);
    selection-color: var(--bg-secondary);
}

QHeaderView::section {
    background-color: var(--bg-tertiary);
    border: 1px solid var(--border-color);
    color: var(--fg-primary);
    padding: 4px;
}

/* Меню */
QMenuBar {
    background-color: var(--bg-primary);
    border-bottom: 1px solid var(--border-color);
    color: var(--fg-primary);
}

QMenuBar::item:selected {
    background-color: var(--accent-color);
    color: var(--bg-secondary);
}

QMenu {
    background-color: var(--bg-primary);
    border: 1px solid var(--border-color);
    color: var(--fg-primary);
}

QMenu::item:selected {
    background-color: var(--accent-color);
    color: var(--bg-secondary);
}

/* Док-виджеты */
QDockWidget {
    titlebar-close-icon: url(:/icons/close_light.png);
    titlebar-normal-icon: url(:/icons/undock_light.png);
}

QDockWidget::title {
    background-color: var(--bg-tertiary);
    padding-left: 5px;
    padding-top: 2px;
}

/* Терминал */
QPlainTextEdit#terminal {
    background-color: var(--terminal-bg);
    color: var(--fg-secondary);
    font-family: "Consolas", "Courier New", monospace;
}

/* Редактор кода */
QPlainTextEdit#codeEditor {
    background-color: var(--bg-secondary);
    color: var(--fg-primary);
    font-family: "Consolas", "Courier New", monospace;
    selection-background-color: var(--selection-bg);
    selection-color: var(--fg-secondary);
}

/* Строка состояния */
QStatusBar {
    background-color: var(--bg-tertiary);
    color: var(--fg-primary);
}

QStatusBar::item {
    border: none;
}

/* Панель инструментов */
QToolBar {
    background-color: var(--bg-primary);
    border-bottom: 1px solid var(--border-color);
    spacing: 3px;
}

QToolButton {
    background-color: transparent;
    border: 1px solid transparent;
    border-radius: 4px;
    padding: 3px;
}

QToolButton:hover {
    background-color: var(--accent-color);
    border: 1px solid var(--border-color);
    color: var(--bg-secondary);
}

QToolButton:pressed {
    background-color: var(--accent-color-pressed);
    color: var(--bg-secondary);
}

