# GopiAI MCP Server

Полнофункциональный MCP (Model Context Protocol) сервер с интеграцией Smart Workspace Indexer и унифицированными инструментами.

## Возможности

### 🚀 Основные функции
- **Автоматическая индексация рабочего пространства** при установке через `setWorkspace`
- **Унифицированные инструменты** для управления задачами и файловой системы
- **Интеграция с Python workspace indexer** для анализа проектов
- **Поддержка Model Context Protocol** для взаимодействия с LLM

### 🛠️ Доступные инструменты

#### 1. setWorkspace
Устанавливает рабочее пространство и автоматически индексирует его.

```json
{
  "name": "setWorkspace",
  "arguments": {
    "path": "/path/to/workspace"
  }
}
```

#### 2. workspace_info
Получает информацию о текущем рабочем пространстве.

```json
{
  "name": "workspace_info",
  "arguments": {}
}
```

#### 3. workspace_context
Получает полный контекст рабочего пространства для LLM.

```json
{
  "name": "workspace_context", 
  "arguments": {}
}
```

#### 4. workspace_search
Поиск файлов в рабочем пространстве по паттерну.

```json
{
  "name": "workspace_search",
  "arguments": {
    "pattern": "*.js",
    "maxResults": 20
  }
}
```

#### 5. workspace_refresh
Обновляет индекс рабочего пространства.

```json
{
  "name": "workspace_refresh",
  "arguments": {
    "force": false
  }
}
```

#### 6. task_manage
Унифицированный инструмент управления задачами (объединяет 8 операций).

```json
{
  "name": "task_manage",
  "arguments": {
    "action": "list",
    "data": {
      "format": "human"
    }
  }
}
```

**Доступные действия:**
- `add` - добавление задач
- `add_subtask` - добавление подзадач
- `list` - список задач
- `update` - обновление задач
- `update_status` - обновление статуса
- `remove` - удаление задач
- `context` - получение контекста
- `next` - следующая задача

#### 7. file_system
Унифицированный инструмент файловой системы (объединяет 11 операций).

```json
{
  "name": "file_system",
  "arguments": {
    "action": "read",
    "data": {
      "path": "package.json"
    }
  }
}
```

**Доступные действия:**
- `read` - чтение файлов
- `write` - запись файлов
- `copy` - копирование
- `move` - перемещение
- `delete` - удаление
- `list` - список директорий
- `create` - создание директорий
- `tree` - дерево файлов
- `search` - поиск файлов
- `info` - информация о файлах
- `status` - статус системы

## Установка и запуск

### Требования
- Node.js 16+
- Python 3.8+ с установленными GopiAI-Extensions

### Установка зависимостей
```bash
cd src
npm install
```

### Запуск сервера
```bash
# Через Node.js
node mcp_server.js

# Через batch файл (Windows)
../start_mcp_server.bat
```

### Тестирование
```bash
node test_mcp_server.js
```

## Архитектура

### Компоненты
- **MCP Server** (Node.js) - основной сервер с поддержкой Model Context Protocol
- **Unified Tools** - унифицированные инструменты с action-based архитектурой
- **Python Bridge** - интеграция с Python workspace indexer через временные скрипты
- **Workspace Indexer** - интеллектуальная система анализа проектов

### Структура файлов
```
src/
├── mcp_server.js              # Основной MCP сервер
├── package.json               # Зависимости Node.js
├── test_mcp_server.js         # Тестовый скрипт
├── README.md                  # Документация
└── tools/
    ├── unified_task_tool.js       # Унифицированный инструмент задач
    └── unified_filesystem_tools.js # Унифицированный инструмент ФС
```

## Интеграция с workspace indexer

Сервер автоматически интегрируется с Python workspace indexer из GopiAI-Extensions:

1. При вызове `setWorkspace` запускается индексация проекта
2. Определяется тип проекта, языки, технологии, фреймворки
3. Строится дерево файлов с учётом .gitignore
4. Результаты кэшируются для быстрого доступа
5. Предоставляется контекст для LLM

### Поддерживаемые типы проектов
- **Node.js** (JavaScript/TypeScript)
- **Python** (Django, Flask, FastAPI)
- **Java** (Maven, Gradle)
- **C#** (.NET)
- **C++** (CMake, Make)
- **Rust** (Cargo)
- **Go** (Go modules)
- **PHP** (Composer)
- **Ruby** (Bundler)
- **Swift** (Package Manager)

## Примеры использования

### Установка рабочего пространства
```javascript
const result = await server.handleSetWorkspace({ 
  path: "/path/to/project" 
});
// Автоматически индексирует проект и возвращает информацию
```

### Получение информации о проекте
```javascript
const info = await server.handleWorkspaceInfo({});
// Возвращает тип проекта, языки, технологии, размер
```

### Поиск файлов
```javascript
const files = await server.handleWorkspaceSearch({ 
  pattern: "*.py", 
  maxResults: 10 
});
// Находит Python файлы в проекте
```

### Управление задачами
```javascript
const task = await server.handleTaskManage({
  action: "add",
  data: {
    title: "Новая задача",
    priority: "high"
  }
});
// Добавляет новую задачу с высоким приоритетом
```

## Производительность

- **Кэширование**: результаты индексации кэшируются на 5 минут
- **Оптимизация**: унифицированные инструменты сокращают количество вызовов на 76%
- **Быстрый доступ**: информация о проекте доступна мгновенно после индексации

## Логирование

Сервер выводит отладочную информацию в stderr:
- Запуск сервера
- Результаты индексации
- Ошибки выполнения инструментов

## Совместимость

- **MCP Protocol**: полная поддержка спецификации
- **OpenAI Function Calling**: совместимые схемы инструментов
- **Cross-platform**: работает на Windows, macOS, Linux

## Разработка

### Добавление новых инструментов
1. Создайте новый инструмент в `tools/`
2. Добавьте его в `mcp_server.js`
3. Обновите схему в `ListToolsRequestSchema`
4. Добавьте обработчик в `CallToolRequestSchema`

### Тестирование
```bash
# Запуск всех тестов
node test_mcp_server.js

# Тестирование отдельного инструмента
node -e "
const server = require('./mcp_server.js');
server.handleSetWorkspace({path: '.'}).then(console.log);
"
```

## Лицензия

MIT License - см. файл LICENSE для деталей.