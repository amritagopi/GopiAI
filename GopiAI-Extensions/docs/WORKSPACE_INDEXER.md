# Smart Workspace Indexer

Интеллектуальная система индексации рабочего пространства для GopiAI, которая автоматически анализирует проекты и предоставляет контекст для LLM.

## 🚀 Возможности

### Определение типа проекта
- **Поддерживаемые типы**: Node.js, Python, Java, C#, C++, Rust, Go, PHP, Ruby, Swift
- **Автоматическое определение** основного языка программирования
- **Анализ зависимостей** и менеджеров пакетов

### Обнаружение технологий
- **Фреймворки**: React, Vue.js, Angular, Django, Flask, FastAPI, Spring, Express.js и др.
- **Инструменты сборки**: Webpack, Rollup, Vite, Gulp, Grunt, Make, CMake, Gradle, Maven
- **Технологии**: Docker, Kubernetes, Terraform, ESLint, Prettier, Jest, pytest

### Построение дерева файлов
- **Поддержка .gitignore** - автоматическое исключение ненужных файлов
- **Умные паттерны игнорирования** - системные файлы, кэш, временные файлы
- **Ограничение глубины** для оптимизации производительности

### Система кэширования
- **TTL кэширование** с настраиваемым временем жизни (по умолчанию 5 минут)
- **Автоматическая инвалидация** при изменении файлов
- **Оптимизация производительности** - повторная индексация за миллисекунды

## 📦 Установка

Smart Workspace Indexer входит в состав GopiAI-Extensions:

```bash
pip install -e ./GopiAI-Extensions
```

## 🔧 Использование

### Базовое использование

```python
from gopiai.extensions.workspace_indexer import get_workspace_indexer

# Получаем индексатор
indexer = get_workspace_indexer()

# Индексируем рабочее пространство
workspace_index = indexer.index_workspace("/path/to/project")

# Получаем краткое описание проекта
summary = indexer.get_project_summary(workspace_index)
print(summary)  # "Проект: node (javascript) | Файлов: 42 | Размер: 1.2 МБ | Фреймворки: React, Express.js"

# Получаем структуру файлов
file_tree = indexer.get_file_tree_summary(workspace_index)
print(file_tree)

# Получаем полный контекст для LLM
llm_context = indexer.get_context_for_llm(workspace_index)
```

### Интеграция с MCP

```python
from gopiai.extensions.mcp_workspace_integration import get_mcp_workspace_integration

# Получаем интеграцию
mcp_integration = get_mcp_workspace_integration()

# Устанавливаем рабочее пространство (автоматически индексирует)
result = mcp_integration.on_workspace_set("/path/to/project")

# Получаем информацию о проекте
project_info = mcp_integration.get_project_info()

# Поиск файлов
search_results = mcp_integration.search_files("*.py", max_results=10)

# Получаем рекомендации по технологиям
recommendations = mcp_integration.get_technology_recommendations()
```

## 🛠️ MCP Инструменты

Smart Workspace Indexer предоставляет следующие MCP инструменты:

### `workspace_info`
Получает информацию о текущем рабочем пространстве.

**Параметры**: нет

**Возвращает**:
```json
{
  "workspace_path": "/path/to/project",
  "project_type": "node",
  "primary_language": "javascript",
  "technologies": ["git", "docker", "eslint"],
  "frameworks": ["React", "Express.js"],
  "total_files": 42,
  "total_size": 1234567
}
```

### `workspace_context`
Получает полный контекст рабочего пространства для LLM.

**Параметры**: нет

**Возвращает**:
```json
{
  "context": "=== КОНТЕКСТ ПРОЕКТА ===\n📊 ОБЩАЯ ИНФОРМАЦИЯ:\n..."
}
```

### `workspace_file_tree`
Получает структуру файлов рабочего пространства.

**Параметры**:
- `max_depth` (number, optional): Максимальная глубина отображения (по умолчанию 3)

**Возвращает**:
```json
{
  "file_tree": "📁 project\n  📄 package.json\n  📁 src\n    📄 index.js\n..."
}
```

### `workspace_search`
Поиск файлов в рабочем пространстве по паттерну.

**Параметры**:
- `pattern` (string): Паттерн для поиска (поддерживает wildcards)
- `max_results` (number, optional): Максимальное количество результатов (по умолчанию 20)

**Возвращает**:
```json
{
  "success": true,
  "pattern": "*.py",
  "results": [
    {
      "name": "main.py",
      "path": "/path/to/project/main.py",
      "is_directory": false,
      "size": 1024
    }
  ],
  "total_found": 5,
  "truncated": false
}
```

### `workspace_recommendations`
Получает рекомендации по технологиям для проекта.

**Параметры**: нет

**Возвращает**:
```json
{
  "success": true,
  "project_type": "python",
  "recommendations": [
    {
      "type": "testing",
      "name": "pytest",
      "reason": "Популярный фреймворк для тестирования Python",
      "priority": "high"
    }
  ]
}
```

### `workspace_refresh`
Обновляет индекс рабочего пространства.

**Параметры**:
- `force` (boolean, optional): Принудительное обновление кэша (по умолчанию false)

### `workspace_clear_cache`
Очищает кэш индексатора рабочего пространства.

**Параметры**: нет

## 📊 Структуры данных

### ProjectInfo
```python
@dataclass
class ProjectInfo:
    project_type: str              # Тип проекта (node, python, java, etc.)
    primary_language: str          # Основной язык программирования
    technologies: List[str]        # Используемые технологии
    frameworks: List[str]          # Фреймворки
    build_tools: List[str]         # Инструменты сборки
    package_managers: List[str]    # Менеджеры пакетов
    config_files: List[str]        # Файлы конфигурации
    entry_points: List[str]        # Точки входа в приложение
    test_directories: List[str]    # Директории с тестами
    documentation_files: List[str] # Файлы документации
```

### FileTreeNode
```python
@dataclass
class FileTreeNode:
    name: str                      # Имя файла/директории
    path: str                      # Полный путь
    is_directory: bool             # Является ли директорией
    size: Optional[int]            # Размер файла (для файлов)
    modified: Optional[float]      # Время последней модификации
    children: Optional[List['FileTreeNode']]  # Дочерние узлы
```

### WorkspaceIndex
```python
@dataclass
class WorkspaceIndex:
    workspace_path: str            # Путь к рабочему пространству
    project_info: ProjectInfo      # Информация о проекте
    file_tree: FileTreeNode        # Дерево файлов
    total_files: int               # Общее количество файлов
    total_size: int                # Общий размер в байтах
    indexed_at: datetime           # Время индексации
    cache_key: str                 # Ключ кэша
```

## ⚙️ Конфигурация

### Настройка кэширования

```python
from gopiai.extensions.workspace_indexer import SmartWorkspaceIndexer
from datetime import timedelta

# Создаём индексатор с кастомным кэшем
indexer = SmartWorkspaceIndexer(cache_dir="/custom/cache/path")

# Настраиваем TTL кэша
indexer.cache.cache_ttl = timedelta(minutes=10)  # 10 минут вместо 5
```

### Настройка паттернов игнорирования

```python
from gopiai.extensions.workspace_indexer import FileTreeBuilder

builder = FileTreeBuilder()

# Добавляем кастомные паттерны игнорирования
builder.default_ignore_patterns.extend([
    '*.backup',
    'temp/',
    'custom_ignore_pattern'
])
```

## 🧪 Тестирование

Запуск тестов:

```bash
cd GopiAI-Extensions
python -m pytest tests/test_workspace_indexer.py -v
```

Создание тестового проекта для демонстрации:

```bash
python examples/workspace_indexer_example.py --create-test
```

## 📈 Производительность

### Оптимизации
- **Кэширование**: Повторная индексация за ~1-5мс при использовании кэша
- **Ограничение глубины**: Максимальная глубина сканирования 10 уровней
- **Умное игнорирование**: Автоматическое исключение ненужных файлов
- **Ленивая загрузка**: Дерево файлов строится по требованию

### Метрики производительности
- **Малые проекты** (< 100 файлов): ~50-100мс
- **Средние проекты** (100-1000 файлов): ~200-500мс  
- **Большие проекты** (1000+ файлов): ~500-2000мс
- **Кэшированные запросы**: ~1-5мс

## 🔍 Поддерживаемые типы проектов

| Тип проекта | Ключевые файлы | Поддерживаемые фреймворки |
|-------------|----------------|---------------------------|
| **Node.js** | package.json, yarn.lock | React, Vue.js, Angular, Express.js, Next.js |
| **Python** | requirements.txt, setup.py, pyproject.toml | Django, Flask, FastAPI, Streamlit |
| **Java** | pom.xml, build.gradle | Spring, Maven, Gradle |
| **C#** | *.csproj, *.sln | .NET, ASP.NET |
| **C++** | CMakeLists.txt, Makefile | CMake, Make |
| **Rust** | Cargo.toml, Cargo.lock | Cargo |
| **Go** | go.mod, go.sum | Go modules |
| **PHP** | composer.json | Laravel, Symfony |
| **Ruby** | Gemfile, *.gemspec | Rails, Sinatra |
| **Swift** | Package.swift, *.xcodeproj | SwiftUI, UIKit |

## 🤝 Интеграция с GopiAI

Smart Workspace Indexer автоматически интегрируется с основной системой GopiAI:

1. **Автоматическая индексация** при установке рабочего пространства
2. **Контекст для LLM** - автоматическое добавление информации о проекте в промпты
3. **MCP инструменты** - доступны через стандартный MCP интерфейс
4. **Кэширование** - оптимизация производительности для больших проектов

## 🐛 Отладка

Включение подробного логирования:

```python
import logging

# Настраиваем логирование для индексатора
logging.getLogger('gopiai.extensions.workspace_indexer').setLevel(logging.DEBUG)

# Создаём обработчик для вывода в консоль
handler = logging.StreamHandler()
handler.setLevel(logging.DEBUG)
formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')
handler.setFormatter(formatter)

logger = logging.getLogger('gopiai.extensions.workspace_indexer')
logger.addHandler(handler)
```

## 📝 Примеры использования

### Анализ React проекта

```python
indexer = get_workspace_indexer()
workspace_index = indexer.index_workspace("/path/to/react-app")

print(f"Тип проекта: {workspace_index.project_info.project_type}")
# Вывод: node

print(f"Фреймворки: {workspace_index.project_info.frameworks}")
# Вывод: ['React']

print(f"Инструменты сборки: {workspace_index.project_info.build_tools}")
# Вывод: ['Webpack']
```

### Анализ Python проекта

```python
workspace_index = indexer.index_workspace("/path/to/django-app")

print(f"Основной язык: {workspace_index.project_info.primary_language}")
# Вывод: python

print(f"Фреймворки: {workspace_index.project_info.frameworks}")
# Вывод: ['Django']

print(f"Точки входа: {workspace_index.project_info.entry_points}")
# Вывод: ['manage.py', 'wsgi.py']
```

## 🔄 Обновления и миграция

При обновлении Smart Workspace Indexer:

1. **Очистка кэша**: Рекомендуется очистить кэш после обновления
2. **Новые типы проектов**: Автоматически поддерживаются без дополнительной настройки
3. **Обратная совместимость**: Сохраняется для всех публичных API

```python
# Очистка кэша после обновления
indexer = get_workspace_indexer()
indexer.clear_cache()
```

## 📞 Поддержка

Если у вас возникли проблемы с Smart Workspace Indexer:

1. Проверьте логи на наличие ошибок
2. Убедитесь, что рабочее пространство доступно для чтения
3. Попробуйте очистить кэш: `indexer.clear_cache()`
4. Используйте принудительное обновление: `index_workspace(path, force_refresh=True)`

---

**Smart Workspace Indexer** - мощный инструмент для автоматического анализа проектов, который делает работу с GopiAI более эффективной и контекстно-осведомлённой! 🚀