# 🚀 Руководство по запуску GopiAI

## Простой запуск (рекомендуется)

Теперь GopiAI автоматически запускает сервер CrewAI при старте UI! Никаких танцев с бубнами.

### Вариант 1: Простой скрипт
```bash
./start_simple.sh
```

### Вариант 2: Прямой запуск
```bash
# Активируйте виртуальное окружение (если есть)
source .venv/bin/activate

# Запустите UI (сервер CrewAI запустится автоматически)
python -m gopiai.ui.main
```

## Что происходит автоматически

1. **🔍 Проверка сервера**: UI проверяет, не запущен ли уже сервер CrewAI
2. **🚀 Автозапуск**: Если сервер не найден, UI автоматически запускает его
3. **⏳ Ожидание**: UI ждет до 30 секунд, пока сервер полностью запустится
4. **✅ Готово**: Как только сервер готов, UI продолжает загрузку
5. **🛑 Автоостановка**: При закрытии UI сервер автоматически останавливается

## Логи и диагностика

- **UI логи**: `ui_debug.log`
- **Сервер логи**: Выводятся в консоль UI или в отдельные файлы сервера

## Если что-то пошло не так

### Сервер не запускается
1. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
2. Убедитесь, что файл `GopiAI-CrewAI/crewai_api_server.py` существует
3. Проверьте логи в консоли

### Порт 5051 занят
```bash
# Найти процесс, использующий порт
lsof -i :5051

# Остановить процесс
pkill -f crewai_api_server
```

### Ручной запуск сервера (для отладки)
```bash
cd GopiAI-CrewAI
python crewai_api_server.py
```

## Старый способ (если нужен)

Если по каким-то причинам автоматический запуск не работает, можете использовать старый скрипт:
```bash
./start_linux.sh
```

## Преимущества нового подхода

- ✅ **Один клик**: Запускаете только UI, сервер стартует сам
- ✅ **Автоматическая остановка**: Закрываете UI - сервер тоже останавливается
- ✅ **Проверка состояния**: UI проверяет, что сервер действительно работает
- ✅ **Обработка ошибок**: Если сервер не запустился, UI покажет ошибку
- ✅ **Никаких лишних терминалов**: Всё работает в одном процессе

## API ключи

Для полной функциональности создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
# Отредактируйте .env и добавьте ваши API ключи
```

## ⌨️ Горячие клавиши

GopiAI поддерживает множество удобных горячих клавиш:

- **Enter** - отправить сообщение
- **Shift + Enter** - новая строка
- **Ctrl + C/V/X/A/Z/Y** - стандартные операции с текстом
- **Escape** - очистить поле ввода

Подробное описание всех горячих клавиш смотрите в [KEYBOARD_SHORTCUTS.md](KEYBOARD_SHORTCUTS.md)