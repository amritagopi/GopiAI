# Исправления интерфейса минимальной версии

## Исправленные проблемы

1. **Проблема с размером кнопок в диалоге выбора темы**
   - Кнопки в диалоге выбора темы были слишком маленькими и текст в них обрезался
   - Увеличен размер кнопок до 150x45px с фиксированным размером
   - Добавлены отдельные цветовые стили для разных кнопок (зеленый, синий, красный)
   - Изменен метод apply_theme, чтобы повторно применять собственные стили после изменения темы

2. **Проблема с отображением иконок в главном окне**
   - Иконки не загружались или не отображались корректно из base64
   - Заменены иконки на символы Unicode (🗕, 🗖, 🗗, ✕)
   - Добавлен специальный стиль для корректного отображения символов

3. **Проблема с применением стилей после смены темы**
   - После применения темы стили кнопок сбрасывались
   - Добавлен метод update_window_icons для восстановления символов
   - Подключен этот метод к сигналу themeChanged

## Технические детали реализации

1. **Для исправления проблемы с кнопками в диалоге:**
   ```python
   # Устанавливаем фиксированные размеры для кнопок
   self.apply_button.setFixedSize(150, 45)
   self.ok_button.setFixedSize(150, 45)
   self.cancel_button.setFixedSize(150, 45)
   
   # Добавляем идентификаторы для стилизации
   self.apply_button.setObjectName("saveButton")
   self.ok_button.setObjectName("okButton")
   self.cancel_button.setObjectName("cancelButton")
   ```

2. **Для исправления проблемы с иконками:**
   ```python
   # Используем символы Unicode для кнопок
   self.minimize_button.setText("🗕")
   self.maximize_button.setText("🗖")
   self.restore_button.setText("🗗")
   self.close_button.setText("✕")
   ```

3. **Для исправления проблемы с применением стилей:**
   ```python
   # Повторно применяем стили после изменения темы
   dialog.themeChanged.connect(lambda colors: self.update_window_icons())
   ```

## Обнаруженные и устраненные баги

- Проблемы с форматом PNG в base64-данных (libpng errors)
- Потеря стилей после применения темы через генератор стилей
- Неверные пути к настройкам (двойное указание папки minimal_version)

Теперь интерфейс корректно отображается и сохраняет настройки при применении темы.